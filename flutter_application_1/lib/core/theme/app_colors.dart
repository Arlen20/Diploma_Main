import 'package:flutter/material.dart';

class AppColors {
  // Background gradient (from your Figma style)
  static const bgTop = Color(0xFF2B2352);
  static const bgBottom = Color(0xFF1B1736);

  // Surfaces
  static Color glass = Colors.white.withOpacity(0.10);
  static Color glassBorder = Colors.white.withOpacity(0.14);

  // Text
  static const textOnDark = Colors.white;
  static const textDark = Color(0xFF1C1C27);

  // Accents (adjust later to match Figma)
  static const accent = Color(0xFFF3F0B6); // soft yellow
  static const lavender = Color(0xFFBDA6FF); // purple accent
  static const softWhite = Color(0xFFF3F2FA);

  // Buttons
  static const primaryBtnText = Color(0xFF1B1736);
}
